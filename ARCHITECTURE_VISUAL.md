# Architecture Visualization

## 🏗️ Application Layers

```
┌─────────────────────────────────────────────────────────────┐
│                        Browser / User                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                        │
│                     (src/components/)                        │
│  ┌──────────────┬──────────────┬──────────────┬──────────┐  │
│  │     Auth     │   Dashboard  │    Modules   │    UI    │  │
│  │  Components  │  Components  │  Components  │  Library │  │
│  └──────────────┴──────────────┴──────────────┴──────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    ROUTING LAYER                             │
│                     (src/routes/)                            │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  React Router • Route Guards • URL Management        │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  BUSINESS LOGIC LAYER                        │
│                 (src/hooks/ & src/contexts/)                 │
│  ┌──────────────┬──────────────┬──────────────┬──────────┐  │
│  │   useAuth    │  useProjects │  useAsync    │ useDialog│  │
│  │              │              │              │          │  │
│  │ AuthContext  │ ProjectCtx   │   Custom     │  Modal   │  │
│  └──────────────┴──────────────┴──────────────┴──────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    SERVICE LAYER                             │
│                    (src/services/)                           │
│  ┌──────────────┬──────────────┬──────────────┬──────────┐  │
│  │    Auth      │   Project    │   Poultry    │   Maple  │  │
│  │   Service    │   Service    │   Service    │  Service │  │
│  └──────────────┴──────────────┴──────────────┴──────────┘  │
│                              │                               │
│                     HTTP Client + Auth                       │
│                              │                               │
│                    ┌─────────┴──────────┐                    │
│                    │                    │                    │
│                    ▼                    ▼                    │
│            ┌──────────────┐    ┌──────────────┐             │
│            │  Mock Data   │    │   Real API   │             │
│            │ (Development)│    │ (Production) │             │
│            └──────────────┘    └──────────────┘             │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

### 1. User Action Flow

```
User clicks "Create Project"
        │
        ▼
Component calls useProjectOperations().handleCreateProject()
        │
        ▼
Hook calls projectService.createProject(data)
        │
        ▼
Service checks: isMockMode?
        │
        ├─── Yes ──► Return mock data
        │
        └─── No ──► HTTP call to API
                        │
                        ▼
                   Real backend
```

### 2. Authentication Flow

```
User submits login form
        │
        ▼
SignInScreen calls useAuth().login(email, password)
        │
        ▼
AuthContext calls authService.login()
        │
        ▼
authService makes API call (or returns mock)
        │
        ▼
Store JWT token in localStorage
        │
        ▼
Update AuthContext state with user
        │
        ▼
Router redirects to /projects
        │
        ▼
Protected routes now accessible
```

### 3. Component Lifecycle with Context

```
Component mounts
        │
        ▼
useProjects() hook subscribes to ProjectContext
        │
        ▼
Context has existing data? 
        │
        ├─── Yes ──► Render immediately
        │
        └─── No ──► Fetch data
                        │
                        ▼
                   projectService.getProjects()
                        │
                        ▼
                   Update context state
                        │
                        ▼
                   Component re-renders
```

## 📦 Module Structure Example

```
Poultry Module
│
├── Types (src/types/)
│   ├── Flock
│   ├── EggLog
│   ├── FeedLog
│   └── HealthEvent
│
├── Service (src/services/)
│   └── poultry.service.ts
│       ├── getFlocks()
│       ├── createFlock()
│       ├── logEggs()
│       └── logFeed()
│
├── Context (src/contexts/)
│   └── PoultryContext.tsx
│       ├── State: flocks, eggs, feed
│       └── Actions: CRUD operations
│
├── Hooks (src/hooks/)
│   └── usePoultryOperations.ts
│       └── Business logic + toast notifications
│
├── Components (src/components/poultry/)
│   ├── PoultryDashboard.tsx
│   ├── FlockManagement.tsx
│   ├── EggLogTable.tsx
│   └── AddFlockModal.tsx
│
└── Routes (src/routes/)
    └── /app/modules/poultry/*
```

## 🔐 Authentication Architecture

```
┌──────────────────────────────────────────────────────────┐
│                    User Not Logged In                     │
└──────────────────────────────────────────────────────────┘
                          │
                          ▼
                    Public Routes
                    (/welcome, /signin, /signup)
                          │
                          ▼
                    User logs in
                          │
                          ▼
            ┌─────────────────────────────┐
            │  authService.login()        │
            │  • Validate credentials     │
            │  • Get JWT token            │
            │  • Store in localStorage    │
            │  • Update AuthContext       │
            └─────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────┐
│                    User Logged In                         │
│                                                           │
│  • Token in localStorage                                 │
│  • User object in AuthContext                            │
│  • HTTP client auto-adds token to requests               │
│  • Protected routes accessible                           │
│  • Public routes redirect to /projects                   │
└──────────────────────────────────────────────────────────┘
                          │
                          ▼
                    Protected Routes
            (/projects, /app/*, /onboarding)
                          │
                          ▼
                    User logs out
                          │
                          ▼
            ┌─────────────────────────────┐
            │  authService.logout()       │
            │  • Clear localStorage       │
            │  • Clear AuthContext        │
            │  • Redirect to /welcome     │
            └─────────────────────────────┘
```

## 🔀 State Management Flow

```
┌─────────────────────────────────────────────────────────┐
│                   Application State                      │
│                                                          │
│  ┌────────────────┐         ┌────────────────┐          │
│  │  AuthContext   │         │ ProjectContext │          │
│  ├────────────────┤         ├────────────────┤          │
│  │ • user         │         │ • projects[]   │          │
│  │ • isAuth       │         │ • currentProj  │          │
│  │ • isLoading    │         │ • isLoading    │          │
│  │ • login()      │         │ • create()     │          │
│  │ • logout()     │         │ • update()     │          │
│  │ • register()   │         │ • delete()     │          │
│  └────────────────┘         └────────────────┘          │
│         │                           │                    │
│         └───────────┬───────────────┘                    │
│                     │                                    │
└─────────────────────┼────────────────────────────────────┘
                      │
                      ▼
        ┌─────────────────────────┐
        │   Any Component Can:    │
        │                         │
        │  const { user } =       │
        │    useAuth();           │
        │                         │
        │  const { projects } =   │
        │    useProjects();       │
        └─────────────────────────┘
```

## 🎯 Adding a New Feature - Visual Guide

```
Step 1: Define Type          Step 2: Create Service
┌──────────────┐            ┌──────────────────┐
│ types/       │            │ services/        │
│ index.ts     │            │ feature.service  │
│              │            │                  │
│ interface    │───────────▶│ class Service {  │
│ MyFeature {} │            │   async get()    │
└──────────────┘            │   async create() │
                            └──────────────────┘
       │                             │
       │                             │
       ▼                             ▼
Step 3: Create Context      Step 4: Create Hook
┌──────────────┐            ┌──────────────────┐
│ contexts/    │            │ hooks/           │
│ FeatureCtx   │            │ useFeature       │
│              │            │                  │
│ state: []    │◀───────────│ operations with  │
│ actions      │            │ toast/errors     │
└──────────────┘            └──────────────────┘
       │                             │
       │                             │
       ▼                             ▼
Step 5: Create Component    Step 6: Add Route
┌──────────────┐            ┌──────────────────┐
│ components/  │            │ routes/          │
│ Feature.tsx  │            │ routes.ts        │
│              │            │                  │
│ UI + logic   │◀───────────│ /feature/path    │
└──────────────┘            └──────────────────┘
```

## 🌐 Request/Response Flow

```
Component
    │
    │ useProjectOperations().handleCreateProject(data)
    │
    ▼
Custom Hook
    │
    │ projectService.createProject(data)
    │
    ▼
Service Layer
    │
    │ Check: isMockMode?
    │
    ├─── TRUE ──────────────┐
    │                       │
    │                       ▼
    │               Mock Data Layer
    │                       │
    │                       │ simulateDelay()
    │                       │ generateId()
    │                       │ update mockDatabase
    │                       │
    │                       ▼
    │               Return Mock Data
    │                       │
    └───────────────────────┤
                            │
    ├─── FALSE ─────────────┤
    │                       │
    │                       ▼
    │               HTTP Client
    │                       │
    │                       │ Add auth token
    │                       │ Set headers
    │                       │ Make request
    │                       │
    │                       ▼
    │               Real Backend API
    │                       │
    │                       │ Process request
    │                       │ Validate auth
    │                       │ Save to database
    │                       │
    │                       ▼
    │               JSON Response
    │                       │
    └───────────────────────┘
                            │
                            ▼
                    Service Returns Data
                            │
                            ▼
                    Hook Processes Result
                    • Show toast
                    • Update context
                    • Handle errors
                            │
                            ▼
                    Component Re-renders
                    with new data
```

## 🎨 Component Hierarchy Example

```
App (Router + Providers)
│
├── AuthProvider
│   └── Context: user, login, logout
│
├── ProjectProvider
│   └── Context: projects, currentProject, CRUD
│
└── BrowserRouter
    │
    ├── Public Routes
    │   ├── WelcomeScreen
    │   ├── SignInScreen
    │   └── SignUpScreen
    │
    └── Protected Routes
        │
        ├── ProjectsDashboard
        │   └── Uses: useProjects(), useProjectOperations()
        │
        ├── OnboardingFlow
        │   └── Uses: useProjectOperations(), useNavigation()
        │
        └── MainAppLayout (with Outlet)
            │
            ├── FarmDashboard
            │   └── Uses: useProjects(), useAuth()
            │
            ├── MapScreen
            │
            ├── CalendarScreen
            │
            ├── ModulesScreen
            │
            └── Modules
                ├── MapleDashboard
                │   └── Uses: useMaple() (future)
                │
                └── PoultryDashboard
                    └── Uses: usePoultry() (future)
```

This visual guide should help understand how all the pieces fit together!
